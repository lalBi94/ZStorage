{
  "name": "ZStorage",
  "version": "1.0",
  "description": "Chain list package",
  "scripts": {
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js",
    "doc": "npx jsdoc ./src/ZStorage.js ./src/ZNodes.js",
    "build": "npm run doc && git add * && git commit -m $ && git push"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
